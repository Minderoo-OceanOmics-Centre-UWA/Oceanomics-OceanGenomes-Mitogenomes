{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/oceangenomesmitogenomes/master/assets/schema_input.json",
    "title": "nf-core/oceangenomesmitogenomes pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^OG\\d+[A-Za-z]?(-\\d+)?(?:_HIC[A-Za-z]*\\d*)?$",
                "errorMessage": "Sample name must start with 'OG' followed by numbers, optionally followed by a letter and/or dash-number (e.g., OG820M-1, OG785, OG764)",
                "meta": ["id"],
                "description": "Sample identifier starting with OG followed by numbers"
            },
            "fastq_1": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "\\.(fastq|fq)(\\.gz)?$",
                "errorMessage": "FastQ file for reads 1 must be provided, cannot contain spaces and must have extension '.fq.gz' or '.fastq.gz'",
                "description": "Full path to FastQ file for Illumina short reads 1. File has to be gzipped and have the extension '.fq.gz' or '.fastq.gz'."
            },
            "fastq_2": {
                "errorMessage": "FastQ file for reads 2 cannot contain spaces and must have extension '.fq.gz' or '.fastq.gz'",
                "anyOf": [
                    {
                        "type": "string",
                        "format": "file-path",
                        "exists": true,
                        "pattern": "\\.(fastq|fq)(\\.gz)?$"
                    },
                    {
                        "type": "string",
                        "maxLength": 0
                    }
                ],
                "description": "Full path to FastQ file for Illumina short reads 2. File has to be gzipped and have the extension '.fq.gz' or '.fastq.gz'."
            },
            "sequencing_type": {
                "type": "string",
                "enum": ["hic", "hifi", "ilmn"],
                "errorMessage": "Sequencing type must be one of: hic, hifi, or ilmn",
                "description": "Type of sequencing technology used: hic (Hi-C), hifi (PacBio HiFi), or ilmn (Illumina)"
            },
            "single_end": {
                "anyOf": [
                    { "type": "boolean" },
                    { "type": "string" }
                ],
                "meta": ["single_end"],
                "description": "Whether the sample is single-end (true) or paired-end (false)"
            },
            "original_id": {
                "type": "string",
                "meta": ["original_id"],
                "description": "Original sample identifier before any cleaning or normalization"
            },
            "completion_date": {
                "anyOf": [
                    { "type": "integer" },
                    { "type": "string" }
                ],
                "meta": ["completion_date"],
                "description": "HiFi completion date parsed from filename (YYMMDD)"
            },
            "date": {
                "anyOf": [
                    { "type": "integer" },
                    { "type": "string" }
                ],
                "meta": ["date"],
                "description": "Sequencing date derived from filename or database (YYMMDD)"
            },
            "assembly_prefix": {
                "type": "string",
                "meta": ["assembly_prefix"],
                "description": "Assembly prefix derived from sample id, sequencing type, and date"
            },
            "nominal_species_id": {
                "type": "string",
                "meta": ["nominal_species_id"],
                "description": "Nominal species identifier from the sample table"
            }
        },
        "required": ["sample", "fastq_1", "sequencing_type"]
    }
}
